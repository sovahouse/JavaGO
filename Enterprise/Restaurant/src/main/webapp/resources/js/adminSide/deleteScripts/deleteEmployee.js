//TODO: добавить все функции удаления в отдельные файлы.
/*$(function () {
    $('.delete').click(function (){
        var id = $(this).attr("name");
        var result = {};
        for (var i = 0; i < data.length; i++) {
            if (data[i].id == id) {
                result = data[i];
                break;
            }
        }
        $.ajax({
            url: "/employees/delete",
            type: "POST",
            data: JSON.stringify(result),
            contentType:"application/json",
            dataType:"json"
        });
        location.reload();
    });
});*/
